input UUIDFilter {
  not: UUIDFilter
  and: [UUIDFilter!]
  or: [UUIDFilter!]
  equal: UUID
  notEqual: UUID
  in: [UUID!]
  notIn: [UUID!]
  gt: UUID
  gte: UUID
  lt: UUID
  lte: UUID
  isNull: Boolean
  isNotNull: Boolean
}
